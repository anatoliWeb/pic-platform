# position_drive_adc.X

[Ukrainian version](./README.ua.md)

## Purpose

Closed-loop position drive example. A DC gear motor moves an arm to a requested angle using a
potentiometer as the position sensor (ADC backend of the `position_drive` library).

## Required Source Files

- `main.c`
- `config_bits.c`
- `project_config.h`
- `../../../../core/delay.c`
- `../../../../drivers/analog/adc/adc.c`
- `../../../../drivers/gpio/gpio.c`
- `../../../../drivers/timers/tick/tick.c`
- `../../../../drivers/timers/timer1/timer1.c`
- `../../../../libraries/actuator/position_drive/position_drive.c`
- `../../../../libraries/system/uart_debug/uart_debug.c`

## What It Demonstrates

- `position_drive` init, `move_to_deg`, and non-blocking `process`
- ADC position sensor through the `read_raw` callback (`adc_read`)
- H-bridge direction control through the `motor` callback
- `tick` millisecond time source as the drive clock
- UART debug status output
- automatic move sequence 30 deg -> 120 deg -> loop

## Pins

| PIC pin | Signal | Connects to | Notes |
| --- | --- | --- | --- |
| RA0/AN0 | position feedback | potentiometer wiper | +5V/GND ends |
| RD0 | IN1 | H-bridge input 1 | motor direction |
| RD1 | IN2 | H-bridge input 2 | motor direction |
| RD2 | EN/PWM | optional enable/PWM | not used by default |
| RC6/TX | UART TX | Virtual Terminal RXD | 9600 8N1 |
| VDD/VSS | power | +5V/GND | required |
| MCLR | reset | pull-up 10k | required |

## Proteus Wiring

See `../../../proteus/actuator/position_drive_adc/README.md` for the full simulation wiring.

## UART Debug Wiring

- `RC6/TX` -> Virtual Terminal `RXD`
- `GND` -> Virtual Terminal `GND`
- UART format: `9600 8N1`

## Drive Configuration

- raw range 0..1023 maps to 0..270 degrees
- tolerance 2 degrees
- move timeout 5000 ms
- stuck detection 1000 ms with 4 raw counts minimum movement
- `POSITION_DRIVE_ENABLE_UART_DEBUG=1` enables `PD:*` messages through the debug callback

## How to Build

```cmd
make -f nbproject\Makefile-default.mk SUBPROJECTS= .build-conf
```

Run it from `examples-projects\xc8\actuator\position_drive_adc.X`.

## HEX

- Generated by MPLAB X in `dist/default/production/position_drive_adc.X.production.hex`
- Repository artifact: `examples-projects/hex/xc8/actuator/position_drive_adc.X.production.hex`

## Dynamic Behavior

- on boot the drive is initialized and the current potentiometer position is read
- the arm moves to 30 degrees, waits until the target is reached, then moves to 120 degrees
- every loop iteration `position_drive_process()` is called to keep the control non-blocking
- errors (timeout, stuck, sensor out of range) stop the motor and are reported over UART

## Expected Behavior

On boot the library initializes, reads the current potentiometer position, and then alternates
between 30 degrees and 120 degrees. The Virtual Terminal shows the application state plus the
`PD:*` library messages.

## Notes

- The H-bridge mapping is fixed by wiring: FORWARD = IN1 high, REVERSE = IN2 high.
  Flip `direction_inverted` in the config if the wiring is reversed.
- `RD2` is optional and unused by default because PWM is disabled in this example.

## Known Limitations

- ADC backend only; encoder backend is not implemented yet.
- Bang-bang control only; no PID.
- One target at a time.
- PWM speed output is compiled out by default.

## Status

Ready for Proteus validation.
