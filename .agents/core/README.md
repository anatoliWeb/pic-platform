# Core

Core maps point to shared helper cards used by drivers and libraries.

| Need | Open |
|---|---|
| Compiler abstraction | `.agents/core/compiler.md` |
| Shared types | `.agents/core/types.md` |
| Compile-time config | `.agents/core/config.md` |
| Clock and timebase glue | `.agents/core/timebase.md` |

## Status

Core docs are mapper-first. They point to the real code and explain what belongs in `core/` versus a driver or library.
