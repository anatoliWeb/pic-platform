# lcd_hd44780

## Purpose

HD44780 character LCD driver with a GPIO 4-bit interface (`lcd_*`)
and a PCF8574 I2C backpack transport (`lcd_i2c_*`).

## Location

```text
 libraries/display/lcd_hd44780/lcd.h
libraries/display/lcd_hd44780/lcd_hd44780.h
libraries/display/lcd_hd44780/lcd.c
libraries/display/lcd_hd44780/lcd_i2c.h
libraries/display/lcd_hd44780/lcd_i2c.c
XC8/libraries/display/lcd_hd44780/lcd.c
C18/libraries/display/lcd_hd44780/lcd.c
```

## Use when

- you need reusable LCD character display support;
- you need an HD44780 behind a PCF8574 I2C backpack (`lcd_i2c_*`).

## Do not use when

- another display family is required.

## Public API

| Function/type/macro | Exact signature/type | Purpose | Arguments | Returns/output | Side effects | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `lcd_init_pins` | `void lcd_init_pins(...);` | bind pins | hardware pins | none | configures pins | exact args in header |
| `lcd_init` | `void lcd_init(void);` | initialize LCD | none | none | initializes controller |  |
| `lcd_clear` | `void lcd_clear(void);` | clear LCD | none | none | sends clear command |  |
| `lcd_set_cursor` | `void lcd_set_cursor(uint8_t row, uint8_t col);` | set cursor | row, col | none | sends cursor command |  |
| `lcd_write_char` | `void lcd_write_char(char c);` | write char | character | none | writes data |  |
| `lcd_write_string` | `void lcd_write_string(const char* str);` | write string | string | none | writes data |  |
| `lcd_i2c_init` | `void lcd_i2c_init(uint8_t i2c_addr, uint32_t i2c_clock_hz);` | init I2C and LCD | 7-bit addr, clock Hz | none | owns I2C + controller init | also enables backlight |
| `lcd_i2c_clear` | `void lcd_i2c_clear(void);` | clear LCD | none | none | sends clear command |  |
| `lcd_i2c_home` | `void lcd_i2c_home(void);` | home cursor | none | none | sends home command |  |
| `lcd_i2c_set_cursor` | `void lcd_i2c_set_cursor(uint8_t row, uint8_t col);` | set cursor | row, col | none | sends cursor command | 2-row addressing |
| `lcd_i2c_write_char` | `void lcd_i2c_write_char(char c);` | write char | character | none | writes data |  |
| `lcd_i2c_write_string` | `void lcd_i2c_write_string(const char* str);` | write string | string | none | writes data |  |
| `lcd_i2c_backlight` | `void lcd_i2c_backlight(uint8_t on);` | set backlight | 0/1 | none | writes port byte |  |

## Configuration structures

Not applicable for this module.

## Error/status model

Not applicable for this module.

## Source inclusion strategy

### Shared implementation

```text
libraries/display/lcd_hd44780/lcd.c
libraries/display/lcd_hd44780/lcd_i2c.c
```

### XC8 implementation/wrapper

```text
XC8/libraries/display/lcd_hd44780/lcd.c
```

### C18 implementation/wrapper

```text
C18/libraries/display/lcd_hd44780/lcd.c
```

### Select exactly one route

```text
For the GPIO transport (lcd_*):
For XC8 add:
XC8/libraries/display/lcd_hd44780/lcd.c

For C18 add:
C18/libraries/display/lcd_hd44780/lcd.c

Do not add together:
libraries/display/lcd_hd44780/lcd.c and the selected compiler-specific .c

Reason:
the shared source dispatches to the compiler-specific implementation.

For the I2C transport (lcd_i2c_*):
add libraries/display/lcd_hd44780/lcd_i2c.c plus
drivers/communication/i2c/i2c.c and core/delay.c.
```

## Required files

```text
libraries/display/lcd_hd44780/lcd.h
libraries/display/lcd_hd44780/lcd_hd44780.h
libraries/display/lcd_hd44780/lcd.c
libraries/display/lcd_hd44780/lcd_i2c.h
libraries/display/lcd_hd44780/lcd_i2c.c
```

## Dependency classification

### Direct compile-time dependencies

```text
core/compiler.h
core/types.h
core/delay.h (lcd_i2c transport)
drivers/communication/i2c/i2c.h (lcd_i2c transport)
```

### Callback-provided dependencies

Not applicable for this module.

### Optional feature dependencies

Not applicable for this module.

### Example-only dependencies

Not applicable for this module.

### Transitive dependencies

Not applicable for this module.

## Compile-time defines

Not applicable for this module.

## Runtime model

LCD operations are synchronous controller writes.

## ISR requirements

Not applicable for this module.

## Resource ownership

LCD pins are project-owned.

## Integration recipe

1. Add the shared LCD source or selected compiler-specific source.
2. GPIO transport: bind pins, then initialize the controller.
3. I2C transport: add `lcd_i2c.c`, `i2c.c` and `core/delay.c`; call `lcd_i2c_init()`.
4. Write characters or strings.

## Minimal usable example

```c
lcd_i2c_init(0x27u, 100000u);
lcd_i2c_clear();
lcd_i2c_write_string("OK");
```

## Working examples

Not applicable for this module.

## Proteus integration

Not applicable for this module.

## HEX artifacts

Not applicable for this module.

## Human documentation

Not applicable for this module.

## Validation status

Implemented in source; validation evidence not tracked in `.agents/`.

## Known limitations

Character LCDs are limited to controller capabilities.

## Extension points

Keep display formatting helpers in reusable wrappers.

## Common mistakes

- Compiling wrapper and shared source together.
- Assuming graphics primitives exist.

## AI decision rule

Use this driver for HD44780 character LCD support.
