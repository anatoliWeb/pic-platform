# ds12c887

## Purpose

DS12C887 RTC driver.

## Location

```text
libraries/time/ds12c887/ds12c887.h
libraries/time/ds12c887/ds12c887.c
```

## Use when

- you need reusable DS12C887 RTC control.

## Do not use when

- another RTC chip is used.

## Public API

| Function/type/macro | Exact signature/type | Purpose | Arguments | Returns/output | Side effects | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `ds12c887_status_t` | enum | status | status values | status | none |  |
| `ds12c887_t` | struct | state | runtime state | state | none |  |
| `ds12c887_init` | `uint8_t ds12c887_init(...);` | initialize RTC | config | success flag | stores config | exact args in header |
| `ds12c887_set_datetime` | `ds12c887_status_t ds12c887_set_datetime(...);` | set datetime | rtc, datetime | status | writes chip | exact args in header |
| `ds12c887_get_datetime` | `ds12c887_status_t ds12c887_get_datetime(...);` | get datetime | rtc, datetime | status | reads chip | exact args in header |
| `ds12c887_read_cmos` | `uint8_t ds12c887_read_cmos(...);` | read CMOS | rtc, addr, data | status | reads chip RAM | exact args in header |
| `ds12c887_write_cmos` | `uint8_t ds12c887_write_cmos(...);` | write CMOS | rtc, addr, data | status | writes chip RAM | exact args in header |
| `ds12c887_enable_periodic_irq` | `void ds12c887_enable_periodic_irq(...);` | enable periodic IRQ | rtc | none | configures chip | exact args in header |
| `ds12c887_get_driver` | `const rtc_driver_t* ds12c887_get_driver(void);` | get RTC driver | none | driver ptr | none |  |

## Configuration structures

Public fields are the state struct described in the header.

## Error/status model

RTC operations return status enums.

## Source inclusion strategy

### Shared implementation

```text
libraries/time/ds12c887/ds12c887.c
```

### XC8 implementation/wrapper

```text
Not applicable for this module.
```

### C18 implementation/wrapper

```text
Not applicable for this module.
```

### Select exactly one route

```text
For XC8 add:
libraries/time/ds12c887/ds12c887.c

For C18 add:
libraries/time/ds12c887/ds12c887.c

Do not add together:
not applicable

Reason:
this module is independent.
```

## Required files

```text
libraries/time/ds12c887/ds12c887.h
libraries/time/ds12c887/ds12c887.c
```

## Dependency classification

### Direct compile-time dependencies

```text
core/compiler.h
core/types.h
```

### Callback-provided dependencies

Not applicable for this module.

### Optional feature dependencies

Not applicable for this module.

### Example-only dependencies

Not applicable for this module.

### Transitive dependencies

Not applicable for this module.

## Compile-time defines

Not applicable for this module.

## Runtime model

RTC operations are synchronous chip transactions.

## ISR requirements

Not applicable for this module.

## Resource ownership

DS12C887 pins and chip state are project-owned.

## Integration recipe

1. Add the shared DS12C887 source.
2. Initialize the device.
3. Read or set time and CMOS.

## Minimal usable example

```c
ds12c887_t rtc;
ds12c887_init(&rtc, &cfg);
```

## Validation status

Implemented in source; validation evidence not tracked in `.agents/`.

## Known limitations

Chip-specific addressing and timing.

## Common mistakes

- Using the wrong RTC chip driver.

## AI decision rule

Use this module for DS12C887 RTC control.
