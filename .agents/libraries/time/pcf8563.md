# pcf8563

## Purpose

PCF8563 RTC driver.

## Location

```text
libraries/time/pcf8563/pcf8563.h
libraries/time/pcf8563/pcf8563.c
```

## Use when

- you need reusable PCF8563 RTC control.

## Do not use when

- another RTC chip is used.

## Public API

| Function/type/macro | Exact signature/type | Purpose | Arguments | Returns/output | Side effects | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `pcf8563_status_t` | enum | status | status values | status | none |  |
| `pcf8563_t` | struct | state | runtime state | state | none |  |
| `pcf8563_init` | `uint8_t pcf8563_init(...);` | initialize RTC | config | success flag | stores config | exact args in header |
| `pcf8563_set_datetime` | `pcf8563_status_t pcf8563_set_datetime(...);` | set datetime | rtc, datetime | status | writes chip | exact args in header |
| `pcf8563_get_datetime` | `pcf8563_status_t pcf8563_get_datetime(...);` | get datetime | rtc, datetime | status | reads chip | exact args in header |
| `pcf8563_set_alarm` | `pcf8563_status_t pcf8563_set_alarm(...);` | set alarm | rtc, alarm | status | writes chip | exact args in header |
| `pcf8563_clear_alarm` | `pcf8563_status_t pcf8563_clear_alarm(...);` | clear alarm | rtc | status | clears flag | exact args in header |
| `pcf8563_get_driver` | `const rtc_driver_t* pcf8563_get_driver(void);` | get RTC driver | none | driver ptr | none |  |

## Configuration structures

Public fields are the state struct described in the header.

## Error/status model

RTC operations return status enums.

## Source inclusion strategy

### Shared implementation

```text
libraries/time/pcf8563/pcf8563.c
```

### XC8 implementation/wrapper

```text
Not applicable for this module.
```

### C18 implementation/wrapper

```text
Not applicable for this module.
```

### Select exactly one route

```text
For XC8 add:
libraries/time/pcf8563/pcf8563.c

For C18 add:
libraries/time/pcf8563/pcf8563.c

Do not add together:
not applicable

Reason:
this module is independent.
```

## Required files

```text
libraries/time/pcf8563/pcf8563.h
libraries/time/pcf8563/pcf8563.c
```

## Dependency classification

### Direct compile-time dependencies

```text
core/compiler.h
core/types.h
```

### Callback-provided dependencies

Not applicable for this module.

### Optional feature dependencies

Not applicable for this module.

### Example-only dependencies

Not applicable for this module.

### Transitive dependencies

Not applicable for this module.

## Compile-time defines

Not applicable for this module.

## Runtime model

RTC operations are synchronous chip transactions.

## ISR requirements

Not applicable for this module.

## Resource ownership

PCF8563 pins and chip state are project-owned.

## Integration recipe

1. Add the shared PCF8563 source.
2. Initialize the device.
3. Read or set time and alarms.

## Minimal usable example

```c
pcf8563_t rtc;
pcf8563_init(&rtc, &cfg);
```

## Validation status

Implemented in source; validation evidence not tracked in `.agents/`.

## Known limitations

Chip-specific addressing and timing.

## Common mistakes

- Using the wrong RTC chip driver.

## AI decision rule

Use this module for PCF8563 RTC control.
