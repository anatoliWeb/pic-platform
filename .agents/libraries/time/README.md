# Time helpers

| Need | Use | Details |
|---|---|---|
| Millisecond tick / timeouts | `tick` driver | `.agents/core/timebase.md` and `.agents/drivers/timers.md` |
| Higher-level scheduling libraries | search catalog first | `time/` libraries may appear later or already exist in docs |

## Note

`tick` is implemented as a driver in this repository, not as a library. Use the core and driver maps for its detailed usage.

For timeout-driven consumers, open:

- `.agents/libraries/input/button.md`
- `.agents/libraries/actuator/position_drive.md`
